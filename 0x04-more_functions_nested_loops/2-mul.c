#include "main.h"
/**
 * mul - multiplies two integer.
 * @a: the first character
 * @b: the second character.
 *
 * Return: 0 (exit code).
*/
int mul(int a, int b)
{
	int result;

	result = a * b;
	return (result);
}
