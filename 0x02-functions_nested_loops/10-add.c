#include "main.h"
/**
 * add - addition function
 * @i: the int to add
 * @j: the int to add
 *
 * Return: the total of the  two integer
*/
int add(int i, int j)
{
	int total;

	total = i + j;
	return (total);
}
