#include <stdio.h>

/**
 * main - Entry point
 *
 * Description: C program prints the alphabet in
 * lowercase
 *
 * Return: 0 (Success)
*/

int main(void)
{
	int ABC;

for (ABC = 'a'; ABC <= 'z'; ABC++)
putchar(ABC);
putchar('\n');
return (0);
}
