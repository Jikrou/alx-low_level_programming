#include <stdio.h>
/**
 * main - Entry point
 *
 * Description: C program that prints lowercase and uppercase letters
 * of alphabet
 *
 * Return: 0 (Success)
*/
int main(void)
{
for (int i = 'a'; i <= 'z'; i++)
putchar(i);
for (int j = 'A'; j <= 'Z'; j++)
putchar(j)
putchar('\n');
return (0);
}
